pandas
numpy
yfinance==0.2.31
beautifulsoup4==4.12.2
requests==2.31.0
python-dotenv==1.0.0
# --- trading-agent foundation (Phase 0-2) ---
alpaca-py==0.43.4
pytest==9.0.3
# --- Phase 5: LLM overlay (Gemini primary, Groq secondary, Claude last-resort) ---
google-genai>=1.0.0
groq>=0.11.0
anthropic>=0.40.0
# NOTE: indicators are implemented natively in trader/strategy/indicators.py.
# pandas-ta is intentionally NOT used: its current release requires Python >=3.12
# and pulls in the removed numpy.NaN symbol, which breaks on this stack.
# --- Crypto trading via CCXT (multi-exchange) ---
ccxt>=4.3.0
# --- Phase 9: Postgres support for Render two-service deployment ---
psycopg2-binary>=2.9
alembic>=1.13
# --- Phase 8: FastAPI backend (replaces Streamlit) ---
pydantic>=2.0
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
PyJWT>=2.8.0
bcrypt>=4.1.0
python-multipart>=0.0.9
email-validator>=2.0
tzdata>=2024.1
